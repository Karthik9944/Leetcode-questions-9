
#include <stdio.h>

int main()
{
    int i,n;
    printf("Enter:");
    scanf("%d",&n);
    if(i=n^3,i>0 && i<=n)
    {
      printf("True");
    }
    else
    {
        printf("False");
    }
    return 0;
}
